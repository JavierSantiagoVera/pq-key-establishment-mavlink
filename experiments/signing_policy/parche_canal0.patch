diff --git a/libraries/GCS_MAVLink/GCS_Signing.cpp b/libraries/GCS_MAVLink/GCS_Signing.cpp
index 44396eaf15..8390cb37b1 100644
--- a/libraries/GCS_MAVLink/GCS_Signing.cpp
+++ b/libraries/GCS_MAVLink/GCS_Signing.cpp
@@ -115,11 +115,11 @@ static const uint32_t accept_list[] = {
     
 static bool accept_unsigned_callback(const mavlink_status_t *status, uint32_t msgId)
 {
-    if (status == mavlink_get_channel_status(MAVLINK_COMM_0)) {
-        // always accept channel 0, assumed to be secure channel. This
-        // is USB on ChibiOS boards
-        return true;
-    }
+    // PARCHE EXPERIMENTAL 2026-09-17 (04_experiments/signing_policy/):
+    // se quita la excepcion de "canal 0 = USB, siempre seguro" porque en el
+    // Bebop el canal 0 es el enlace WiFi (ver HALLAZGO_firma_no_exigida.md).
+    // Sin esto, un COMMAND_LONG sin firmar por el canal 0 se aceptaba igual.
+    (void)status;
     for (uint8_t i=0; i<ARRAY_SIZE(accept_list); i++) {
         if (accept_list[i] == msgId) {
             return true;
